{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd nextjs && npm ci && npm run build && echo 'Build completed, checking standalone...' && (test -d .next/standalone && echo '✅ Standalone directory exists' || echo '❌ Standalone directory missing') && node copy-assets.js"
    },
    "deploy": {
        "startCommand": "cd nextjs && node start-server.js",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
    }
}

