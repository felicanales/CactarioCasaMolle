{
    "$schema": "https://railway.app/railway.schema.json",
    "deploy": {
        "startCommand": "python start.py",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3,
        "healthcheckPath": "/health",
        "healthcheckTimeout": 60
    },
    "build": {
        "builder": "NIXPACKS"
    },
    "$comment": "Using NIXPACKS instead of DOCKERFILE to avoid path issues. If you prefer Dockerfile, change builder to 'DOCKERFILE' and ensure Root Directory = 'fastapi/' in Railway settings."
}