/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Las variables NEXT_PUBLIC_* se leen automáticamente desde .env.local
  // No necesitamos definirla aquí con un valor por defecto
};

export default nextConfig;

